services:
  api:
    image: payment-microservice:latest
    ports:
      - "3667:3667"
    environment:
      - PORT=3667
      - NODE_ENV=production
    volumes:
      - payment-data:/app/data
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 10s
        order: start-first
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3667/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  payment-data:

